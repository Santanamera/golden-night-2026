[phases.setup]
nixPkgs = ["php82", "php82Extensions.pdo", "php82Extensions.pdo_mysql", "php82Extensions.curl", "php82Extensions.mbstring", "php82Extensions.fileinfo", "php82Extensions.gd", "nginx"]

[phases.build]
cmds = []

[start]
cmd = "chmod +x start.sh && APP_ROOT=/app ./start.sh"
