# Prevent directory listing
Options -Indexes

# Block direct access to PHP files in uploads
<Files "*.php">
    Order Deny,Allow
    Deny from all
</Files>

# Allow image and PDF files only
<FilesMatch "\.(jpg|jpeg|png|gif|webp|pdf)$">
    Order Allow,Deny
    Allow from all
</FilesMatch>
